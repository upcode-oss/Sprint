import { ProjectFrame } from "@/features/projects/project-context";
export default function ProjectLayout({ children }: { children: React.ReactNode }) { return <ProjectFrame>{children}</ProjectFrame>; }

